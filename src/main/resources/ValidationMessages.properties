com.graylog.agent.config.constraints.IsOneOf.message="${validatedValue}" is not one of: ${validValues}
com.graylog.agent.config.constraints.IsAccessible.message=${file} or ${dir} is not accessible (check if directory exists and permissions are correct)